import {IOptions} from './type'

export const DEFAULT_OPTIONS : IOptions = Object.freeze({
  cache: true,
  base: 'emoji',
  scale: 0.5,
  speed: 10,
  density: 3,
  staggered: true,
  increaseSpeed: 0.08,
  emoji: null
})

